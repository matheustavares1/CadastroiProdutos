spring:
  application:
    name: Products
  datasource:
    url: jdbc:postgresql://database:5432/registerProducts
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
